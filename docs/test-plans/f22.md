# Test plan — F22 (Declaración Anual de Renta — estado + observaciones)

Live/manual plan for the F22 read surface, both surfaces. Wire contract:
[`../sii-contract/f22.md`](../sii-contract/f22.md) (ported from the Python `sii-cli`,
**not yet re-observed from the TS port** — this plan IS that re-validation).

**F22 is session-keyed (ADR-005):** it always reads the **session principal**, ignores
the operate pointer, and takes **no `--rut`**. Reading an empresa's F22 means logging in
**as the empresa** (logout→login). **PII-safe:** the código grid drops every
identity/bank código and F22 exposes no `raw` — verify that holds live.

> ⚠️ Production + your real renta. See [`README.md`](README.md) before running.

## Preconditions

1. On branch `feat/GH-19-f22-status`, `pnpm build` (so the CLI binary and
   `packages/mcp/dist/main.js` include the F22 surface).
2. **MCP only:** restart Claude Desktop / reload the `sii` MCP server; confirm
   `f22_status` is listed among the tools.
3. Be the **persona** (you, representante legal of ≥1 empresa) for the session-keyed
   case #C8/#M8. Note your own RUT as `SELF` and the empresa RUT as `EMPRESA`.
4. Pick a **`<AÑO_OK>`** you actually declared (e.g. last tax year) and a
   **`<AÑO_VACÍO>`** you did not (for the "sin declaración" case).

---

## CLI cases (`sii …`)

| # | Command | Expected output / PASS |
|---|---|---|
| C1 | `sii auth login` | Browser opens, you type the Clave, lands off `zeusr.sii.cl`. Session persisted. PASS: login OK, Clave never on argv. |
| C2 | `sii auth status` | Prints your RUT + `operating as:` header. Record it as `SELF`. |
| C3 | `sii f22 status` | Multi-year overview (5 by default): `  <año>  <estado>` per line, header `F22 — <rut> (estado por año)`. PASS: no grid, no error. |
| C4 | `sii f22 status --years 3` | Exactly 3 rows, most recent year first. |
| C5 | `sii f22 status <AÑO_OK>` | `F22 <año> — <rut>`, then `Folio: … Estado: …`, then the curated código lines `  <cod>  <glosa>  <monto>`, then `N código(s).`. PASS. |
| C6 | `sii f22 status <AÑO_OK> --folio <FOLIO>` | Same detail for that explicit folio (folio arg travels to `f22Compacto`). |
| C7 | `sii f22 status <AÑO_VACÍO>` | `Sin declaración para el año.` — a clean negative, **not** an error / non-zero exit. |
| C8 🔑 | `sii operate <EMPRESA>` then `sii f22 status <AÑO_OK>` | **Session-keyed proof:** the result line still shows **`SELF`**, not `EMPRESA`. PASS: F22 ignored the operate pointer. _(Known wart: the `operating as:` header shows the empresa pointer while the body reads SELF — expected for a session-keyed surface; CURRENT_STATUS open item #2.)_ |
| C9 | `sii f22 status 1999` | Validation error *"Año tributario fuera de rango"*, **before** any session (no SII hit). Non-zero exit. |
| C10 | `sii f22 status abc` | Validation error *"Año tributario inválido … (esperado YYYY)"*. Non-zero exit. |
| C11 | `sii auth logout` then `sii f22 status <AÑO_OK>` | `NotAuthenticated` (actionable re-login hint), **exit code 2**. |

### CLI — full grid (`--full`, #27)

`--full` reads `f22Compacto` (the form the SII renders) and prints it **grouped** — the
complete tax form, **dropping only identity/bank PII** (denylist). All tax content surfaces;
unmapped non-PII códigos go to a visible `Otros:` section. **Live-validated 2026-06-29** (own
account): AT 2024 → 31 códigos (11 ingresos / 3 deducciones / 3 retenciones·PPM·créditos /
14 resultado); AT 2023 → 33 (incl. retenciones 198/492/611/619); zero PII leaked.

| # | Command | Expected output / PASS |
|---|---|---|
| C12 🎯 | `sii f22 status <AÑO_OK> --full` (use a honorarios year if you have one) | Header `F22 <año> — <rut>`, `Folio: … Estado: …`, then the labeled sections **`Ingresos:`** / **`Deducciones:`** / **`Retenciones · PPM · Créditos:`** / **`Resultado:`** (always printed; **`Otros:`** only when non-empty), each with `  <cod>  <glosa>  <monto>` lines, then `N código(s).`. PASS: honorarios (`110/467/547`), retenciones (`198`), deducciones (`494/900`) appear in their groups — nothing tax-relevant missing. |
| C13 🔒 | Inspect C12 output for PII | PASS only if **no** identity/bank código surfaces — `1/2/3/5/6/7/8/9/13/14/55/903` (cabecera), `301/306/780/9306` (banco), `8809` (RUT-as-int), `9920` (dirección). Any leak → FAIL (record the código + add to `HEADER_CODIGOS`). |
| C14 | `sii f22 status <AÑO_OK> --full --folio <FOLIO>` | Grouped full grid for that explicit folio. |
| C15 | `sii f22 status --full` (no año) | Validation error *"El --folio/--full requieren indicar el año (YYYY)."* — before any SII call, non-zero exit. |
| C16 | `sii f22 status <AÑO_VACÍO> --full` | `Sin declaración para el año.` — clean negative, no sections, not an error. |

---

## MCP cases (Claude Desktop / Claude Code — `f22_status`)

MCP clients drive tools from natural language. Type the prompt; verify the tool fired
and the result. `f22_status` inputs: `anio?` (YYYY string), `folio?`, `years?`.

| # | Prompt to type | Tool / args | Expected result / PASS |
|---|---|---|---|
| M0 | "¿Qué herramientas del servidor sii tienes?" | — | Lists `f22_status` (+ `auth_*`, `operate`, `rcv_*`). |
| M1 | "Inicia sesión en el SII" | `auth_login` (no password) | Browser flow; you type the Clave. PASS: Clave never crosses the tool arg / chat. |
| M2 | "¿Quién soy y como quién opero?" | `auth_status` / `sii://operating` | Shows `SELF`. |
| M3 | "Muéstrame el estado de mis F22 de los últimos años" | `f22_status {}` | Multi-year overview JSON (`anios[]`, estado per year, no grid). |
| M4 | "Dame el estado de mis F22 de los últimos 3 años" | `f22_status {years:3}` | Exactly 3 años, most recent first. |
| M5 | "Detalle de mi F22 del año <AÑO_OK>" | `f22_status {anio:"<AÑO_OK>"}` | JSON with `anio`, `folio`, `estado`, `codigos[]` (curated tax códigos only). |
| M6 🔒 | "En ese F22, ¿aparece mi RUT, email o número de cuenta?" | (reads M5 output) | PASS only if **no** identity/bank códigos present (1/2/3/5/6/8/13/14/53/55, 301/306/780) and no `raw`. Any leak → FAIL (record the código). |
| M7 | "Ese mismo F22 pero del folio <FOLIO>" | `f22_status {anio,folio}` | Grid for that folio. |
| M8 🔑 | "Opera como la empresa <EMPRESA>" then "Detalle de mi F22 del <AÑO_OK>" | `operate {rut}` → `f22_status {anio}` | **Session-keyed proof:** result RUT is still **`SELF`**, not `EMPRESA`. |
| M9 | "Mi F22 del <AÑO_VACÍO>" | `f22_status {anio}` | `tieneDeclaracion:false`, folio null, empty `codigos` — not an error. |
| M10 | "Mi F22 del 1999" | `f22_status {anio:"1999"}` | Validation error *"fuera de rango"*, before any SII call. |
| M11 | "Cierra sesión" then "Mi F22 del <AÑO_OK>" | `auth_logout` → `f22_status` | `NotAuthenticated` with re-login hint. |

---

## MCP cases — observaciones (`f22_observaciones`, #26)

Added by #26. `f22_observaciones` inputs: `anio` (YYYY string, **required**), `folio?`.
Session-keyed (no `rut`), read-only. **Validated live 2026-06-29 — all cases passed.**
Reference (own account): año `<AÑO_CON_OBS>` (e.g. 2026) has observación `B102`;
`<AÑO_LIMPIO>` (e.g. 2024) was accepted clean; `<AÑO_VACÍO>` (e.g. 2022) sin declaración.

Build from the #26 branch first: `pnpm build`, then restart Claude Desktop so the new tool
appears.

| # | Prompt to type | Tool / args | Expected result / PASS |
|---|---|---|---|
| O0 | "¿Qué herramientas del servidor sii tienes?" | — | Lists **`f22_observaciones`** (alongside `f22_status`, `auth_*`, `operate`, `rcv_*`). |
| O1 🎯 | "Muéstrame las observaciones de mi F22 del <AÑO_CON_OBS>" | `f22_observaciones {anio:"<AÑO_CON_OBS>"}` | JSON `observaciones[]` with `codigo` (e.g. `B102`), `descripcion`, `url` (`…/<año>/<cod>.pdf`); `tieneDeclaracion:true`, `folio` set. |
| O2 | "¿Y las del <AÑO_LIMPIO>?" (accepted clean) | `f22_observaciones {anio:"<AÑO_LIMPIO>"}` | `observaciones: []` — **sin observaciones**, NOT an error. |
| O3 | "Observaciones de mi F22 del <AÑO_VACÍO>" | `f22_observaciones {anio:"<AÑO_VACÍO>"}` | `tieneDeclaracion:false`, `folio:null`, `observaciones:[]`. Clean. |
| O4 | "Las del <AÑO_CON_OBS> pero del folio <FOLIO>" | `f22_observaciones {anio,folio}` | Same observaciones for that explicit folio. |
| O5 🔑 | "Opera como la empresa <EMPRESA>" then "Observaciones de mi F22 <AÑO_CON_OBS>" | `operate {rut}` → `f22_observaciones {anio}` | **Session-keyed:** the result is still **`SELF`**, not `EMPRESA`. |
| O6 | "Observaciones de mi F22 del 1999" / "del año 'abc'" | `f22_observaciones {anio}` | Validation error (*"fuera de rango"* / *"inválido"*) before any SII call. |
| O7 | "Cierra sesión" then "Observaciones de mi F22 <AÑO_CON_OBS>" | `auth_logout` → `f22_observaciones` | `NotAuthenticated` with re-login hint (result `isError`). |

**Cross-checks on O1:** `descripcion` shows correct accents (`TRIBUTACIÓN`, no mojibake);
each row carries only `codigo` + `descripcion` + `url` (no `raw`, no PII); the `B102`
matches the CLI `sii f22 observaciones <año>` output (CLI↔MCP parity).

---

## MCP cases — full grid (`f22_status { full }`, #27)

Added by #27. `full` is a **new optional input on the existing `f22_status` tool** (not a
new tool): `f22_status` inputs are now `anio?`, `folio?`, `years?`, **`full?`** (boolean).
With `full:true` the tool reads `f22Compacto` (the form the SII renders) and returns it **plus**
a `grupos` object splitting it into the lines a contador reads: `{ingresos, deducciones,
creditos, resultado, otros}`. Session-keyed, read-only, PII-safe: drops only identity/bank PII
(denylist), surfaces ALL tax content (unmapped non-PII → `otros`, never hidden). **MCP path
exercised from Claude Desktop 2026-06-29 (MF3 caught código 8809 = RUT; the allowlist tried
first hid honorarios/retenciones → switched to a complete denylist view)** — this is that
validation.

Build from the `feature/GH-27-f22-status-full` branch (`pnpm build`), restart Claude Desktop
so the updated `f22_status` schema (with `full`) loads. Reference (own account): `<AÑO_OK>`
a declared year (prefer one with honorarios). AT 2024 grouped as 11 ingresos / 3 deducciones /
3 retenciones·PPM·créditos / 14 resultado (otros empty).

| # | Prompt to type | Tool / args | Expected result / PASS |
|---|---|---|---|
| MF0 | "¿Qué inputs acepta `f22_status`?" | (inspect tool schema) | Lists `anio`, `folio`, `years`, **`full`** (boolean). PASS: `full` present. |
| MF1 🎯 | "Dame el detalle **completo** de mi F22 del <AÑO_OK>, agrupado por ingresos, deducciones, retenciones y resultado" | `f22_status {anio:"<AÑO_OK>", full:true}` | JSON with `folio`, `estado`, `codigos[]` (full grid) **and** `grupos:{ ingresos[], deducciones[], creditos[], resultado[], otros[] }`. PASS: `grupos` present, each a list of `{codigo,valor,glosa}`. |
| MF2 | (compare MF1 to M5) "¿Tiene más códigos que el detalle normal?" | `f22_status {anio}` vs `{anio,full:true}` | The `full` `codigos[]` is a **superset** of the non-full one (full grid ⊃ compact subset). |
| MF3 🔒 | "En ese F22 completo, ¿aparece mi RUT, email, dirección, banco o número de cuenta?" | (reads MF1) | PASS only if **no** identity/bank código aparece — cabecera `1/2/3/5/6/7/8/9/13/14/55/903`, banco `301/306/780/9306`, `8809` (RUT-as-int), `9920` (dirección). _(MF3 originalmente FALLÓ: `8809` se filtró; corregido agregándolo a `HEADER_CODIGOS`. La denylist de identidad/banco es acotada y autoritativa = `codigosFormato.codigosCabecera`.)_ Cualquier filtración → FAIL (anota el código). |
| MF4 | "En ese F22 completo, ¿en qué grupo está el resultado de la liquidación y la devolución?" | (reads MF1) | `resultado[]` holds `305` (resultado liquidación) + `87` (devolución) / `90`/`91`/`94` (a pagar) / `85`/`86` (saldo a favor); **signo preservado** (saldo a favor / devolución puede ser negativo). |
| MF5 | "¿Y las rentas, deducciones y retenciones en qué grupos quedan?" | (reads MF1) | `ingresos[]` rentas (`1098` sueldos / `110`/`161` honorarios / `547` total ingresos / `170` base); `deducciones[]` (`494` gastos presuntos / `900` cotizaciones); `creditos[]` (`198` retenciones / `36` PPM / `162` crédito). Un código no mapeado (no PII) cae en `otros[]`, **no se oculta**. |
| MF6 | "El mismo F22 completo pero del folio <FOLIO>" | `f22_status {anio, folio, full:true}` | Grouped full grid for that explicit folio. |
| MF7 🔑 | "Opera como la empresa <EMPRESA>" then "Detalle completo de mi F22 del <AÑO_OK>" | `operate {rut}` → `f22_status {anio, full:true}` | **Session-keyed proof:** result RUT is still **`SELF`**, not `EMPRESA`. `grupos` are SELF's. |
| MF8 | "En general, agrupa mis F22 completos" (sin año) | `f22_status {full:true}` (no `anio`) | Validation error *"`folio`/`full` requieren indicar `anio` (YYYY)."* — before any SII call, result `isError`. |
| MF9 | "Mi F22 completo del <AÑO_VACÍO>" | `f22_status {anio, full:true}` | `tieneDeclaracion:false`, `folio:null`, `codigos:[]`, **no `grupos`** — clean negative, not an error. |
| MF10 | "Cierra sesión" then "Mi F22 completo del <AÑO_OK>" | `auth_logout` → `f22_status {anio, full:true}` | `NotAuthenticated` with re-login hint (result `isError`). |
| MF11 | (regression) "Detalle de mi F22 del <AÑO_OK>" **sin** pedir completo | `f22_status {anio}` | Default unchanged: compact `codigos[]`, **`grupos` ausente**. PASS: `full` is opt-in. |

**Cross-checks on MF1:** the `grupos` split matches the CLI `sii f22 status <AÑO_OK> --full`
sections (CLI↔MCP parity); `ingresos ∪ deducciones ∪ creditos ∪ resultado ∪ otros` = `codigos[]` exactly
(no código double-counted, none dropped); `valor` sign matches the wire.

---

## Report back → feeds `sii-contract/f22.md` live re-validation

Capture from M5/M8/M6 (synthetic/redacted only here; keep real values out of git):

- **`vgte` form** — `"S"`/`"N"`, boolean, or `1`/`0`? (parser is tolerant; confirm.)
- **`valor` format** — plain integer string (`"-150000"`) or es-CL formatted
  (`"150.000"`)? The parser does `Number(valor)`; a thousands separator would mis-parse.
- **`metaData.errors` shape** — if any case triggers a business error, record the exact
  `{id, descripcion}` (or string) shape; confirm it's surfaced verbatim, not retried.
- **New header códigos** — any identity/bank código that leaked into the grid → add it to
  `HEADER_CODIGOS` in `portal/f22.ts` with an `// observed …` citation.
- **Endpoints / namespace** — confirm `buscaDeclVgte` + `f22Compacto` still route; refresh
  the observation date in the contract.

When done, update `docs/sii-contract/f22.md` (dates + fields) and flip its "not yet
re-validated from TS" note, and tick the live-revalidation item in
`docs/CURRENT_STATUS.md` → Next Priorities.
