{
  "name": "@sii/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server (stdio). Each tool/resource/prompt is a thin call into @sii/core tasks (ADR-003). Mirrors the CLI surface by default.",
  "bin": {
    "sii-mcp": "./dist/server.js"
  },
  "dependencies": {
    "@sii/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "scripts": {
    "build": "tsc -b"
  }
}
