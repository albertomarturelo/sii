{
  "name": "@sii/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Human-facing CLI. Each command is a thin call into @altumstack/sii-core tasks (ADR-003).",
  "bin": {
    "sii": "./dist/main.js"
  },
  "dependencies": {
    "@altumstack/sii-core": "workspace:*",
    "commander": "^12.1.0"
  },
  "scripts": {
    "build": "tsc -b"
  }
}
